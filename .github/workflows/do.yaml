name: Github Actions 실행하기

on:
  push:
    branch:
      - main:

jobs:
  My-First-CICD-Job:
    runs-on: ubuntu-latest

    steps:
      - name: simple-web
        run: |
          pwd
          ls -al
          echo "This is the first cicd step" >> a.txt
          ls -al
          cat a.txt